program pelos; 
vars
     int i, j, k; 

function int uno (int j)
vars 
    int i; 
{
    i = j + (k - j * 2 + j);
    if(j > 0) then
    {
        write("if statement");
        return(uno(i-j));
    }
    else
    {
        write("else statement");
        return(i+k);
    }
}

function void dos(int i, int j)
vars 
    int x, y;
{

    x = i + j; 
    if(x > j * k) then
    {
        write(uno(x-k));
    }
    else
    {
        write(uno(j));
    }
}

main(){
    k = 2;
    i = 20;
    j = i * 2 + i;
    dos(j-i, j);
}
