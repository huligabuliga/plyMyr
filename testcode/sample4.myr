program functiontest;
vars
    int x;
    bool y;


function bool isGreaterThanTen(int num)
{
    if (num > 10) then {
        return (true);
    } else {
        return (false);
    }
}

main() {
    x = 20;
    y = isGreaterThanTen(x);
    write("y is", y);
}