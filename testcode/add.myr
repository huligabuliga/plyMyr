program functiontest;
vars
    int x, y, c;
    

function int add (int a, int b)
vars
    int dobby; 
{      
        dobby = a + 5;
        dobby = dobby + b;
        return (dobby);
}

main() {
    x = 20;
    y = 40;
    c = add(x, y);
    write("sum is", c);
}