program functiontest;
vars
    int x, y, c;
    

function int add (int a, int b)
vars
    int result; 
{      
        result = a + b;
        return (result);
}

main() {
    x = 20;
    y = 40;
    c = add(x, y);
    write("sum is", c);
}