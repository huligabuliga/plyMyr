program factorial;
vars 
    int num;

function int factorial(int n)
vars
    int result;
{
    if (n == 0) {
        return 1;
    } else {
        return n * factorial(n - 1);
    }
}
   
main(){
  int num = 5;
  write(factorial(num));
}